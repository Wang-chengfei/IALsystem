<view class="container">
	<swiper indicator-dots="{{true}}" autoplay="{{false}}">
		<swiper-item wx:for="{{content}}">
			<image src="{{item.url}}"></image>
      <view class="wenzi">{{item.wenzi}}</view>
		</swiper-item>
	</swiper>
</view>